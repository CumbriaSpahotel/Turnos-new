const fs = require('fs');

const empId = "proximamente";
const resolvedA = "dani";
const resolvedB = "proximamente";

console.log("isOrigin:", empId === resolvedA);
console.log("isDestination:", empId === resolvedB);
